name: ci
on:
  push:
    branches: ['*']
  pull_request:
jobs:
  build:
    strategy:
      matrix:
        node: ['16']
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - node: '14'
            os: ubuntu-latest
          - node: '18'
            os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - if: runner.os == 'Windows'
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
      - run: yarn --frozen-lockfile
      - run: yarn prettier --check .
      - run: yarn lint
      - run: yarn build
  # test-ios:
  #   runs-on: macos-11
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: actions/setup-node@v3
  #       with:
  #         node-version: '16'
  #     - uses: maxim-lobanov/setup-xcode@v1
  #       with:
  #         xcode-version: '13.0'
  #     - run: yarn --frozen-lockfile
  #     - name: Bundle Install
  #       working-directory: ./ios
  #       run: bundle install
  #     - name: Install Dependencies with Pods
  #       working-directory: ./ios
  #       run: bundle exec pod install
  #     - name: Run iOS tests
  #       working-directory: ./ios
  #       run: xcodebuild -workspace "ElectrodeReactNativeBridge.xcworkspace" -scheme "ElectrodeReactNativeBridgeTests" -destination "platform=iOS Simulator,name=iPhone 13"
